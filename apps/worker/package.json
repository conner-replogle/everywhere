{
  "name": "@everywhere/worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler d1 migrations apply DB --remote && wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply DB --local",
    "types": "wrangler types",
    "typecheck": "tsc -p .",
    "reset-password": "bun scripts/reset-password.ts",
    "vapid-key": "bun scripts/vapid-key.ts",
    "test": "bun test test/"
  },
  "dependencies": {
    "@everywhere/protocol": "workspace:*",
    "hono": "^4.13.8"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "wrangler": "^4.137.0"
  }
}
